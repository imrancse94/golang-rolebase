package routes

import (
	"github.com/gofiber/fiber/v2"
)

func SetupAuthRoutes(app *fiber.App) {
	// authGroup := app.Group("/auth")
	// authGroup.Use(middleware.AuthMiddleware) // Protect all routes under /auth
}
